# DiamondCrew Interactive Staff Center 1.3.1

Infrastructure launcher titles now use the complete DiamondCrew Interactive product name for Server Manager, Server Controller, Proxy Manager, Image Service and Services Status. Existing roleplay project names remain unchanged. Brand links and external-card accessible names identify the complete product; Image Service's header and heading use the same full brand, with mobile text wrapping.

This patch changes only visible/accessibility naming, the minimal Image heading wrap style, package version metadata and these notes. URLs, authentication, status sources, deployment flags and stored media are unchanged. Image management remains IN PROGRESS until verified; the existing CDN and /uploads/ paths are not migrated by this patch.
